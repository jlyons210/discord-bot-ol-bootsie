/**
 * A collection of Discord Message intents that controls how messages are treated by the bot
 */
export enum DiscordBotMessageIntent {
  ChatMessage = 'chat-message',
  ImagePrompt = 'image-prompt',
}